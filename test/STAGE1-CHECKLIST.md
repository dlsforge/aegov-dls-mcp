# Stage 1 success criteria — checklist & coverage map

> Derived from CLAUDE.md (hard rules, definition of done) and STAGE1-HANDOFF.md (§3, §7, §10).
> Column "covered by" names the suite that proves the criterion; `test/` items are the new
> `npm test` suite (`node --test`), written for criteria the existing suites did not cover.
> Discipline: tests exercise the REAL built server over stdio via the official MCP client —
> the same machinery assistants embed. A failing test is reported, never papered over.

## A. Exit test (definition of done)

| # | Criterion | Covered by |
|---|-----------|-----------|
| A1 | 10 government screens generated by check-blind MCP-connected agents pass all checks without hand-correction | `npm run evals` (10/10, §10.9) |
| A2 | Outputs gated through `validate_snippet` with zero errors | `npm run evals` (judge gates every output) |

## B. Server & protocol

| # | Criterion | Covered by |
|---|-----------|-----------|
| B1 | Assistant connects over stdio; initialize handshake reports `aegov-dls` | `npm run smoke` |
| B2 | All 7 tools listed: ping, listComponents, getComponent, getTokens, scaffoldUaePass, scaffoldEmiratesId, validate_snippet | `npm run smoke` |
| B3 | ping / ping-echo round-trip | `npm run smoke` |
| B4 | Malformed/missing tool arguments produce a protocol error, never a crash or a wrong answer | `test/edge-cases.test.mjs` |

## C. Catalogue integrity (rules-core)

| # | Criterion | Covered by |
|---|-----------|-----------|
| C1 | 27 components / 223 tokens / 8 blocks / 6 patterns / 2 docs-only; artifact lint + invariants pass | `npm run validate` |
| C2 | Every docs-tier record carries sourceUrl + retrievedOn + trust flag; package tier carries version + extractedFrom | `npm run validate` + smoke assertions |
| C3 | Two provenance tiers surfaced distinctly in tool output | `npm run smoke` (tier fields) |
| C4 | Known docs↔package drift classes are flagged, not validated | `npm run smoke` (aegov-newslette) + `test/` (unquoted form) |

## D. Read tools — edge cases (new)

| # | Criterion | Covered by |
|---|-----------|-----------|
| D1 | getComponent name resolution is case/format-tolerant (`.aegov-btn`, `BUTTON`, `aegov btn`, `check_item`) | `test/edge-cases.test.mjs` |
| D2 | getComponent unknown name → `isError` + knownIdentifiers + didYouMean on near-miss; empty name doesn't crash | smoke (basic) + `test/` (near-miss, empty) |
| D3 | getTokens invalid category → protocol error (enum-constrained) | `test/edge-cases.test.mjs` |
| D4 | getTokens no-match query → `matched: 0`, empty list, not an error | `test/edge-cases.test.mjs` |
| D5 | getTokens unfiltered returns full 223-token set; query is case-insensitive | `test/edge-cases.test.mjs` |

## E. Scaffolders — non-negotiables under every option (new beyond defaults)

| # | Criterion | Covered by |
|---|-----------|-----------|
| E1 | scaffoldEmiratesId: every language option round-trips validate_snippet clean; `en` carries no Arabic, `ar`/`both` carry arabicNote (never silently "improved" Arabic) | `test/edge-cases.test.mjs` (defaults: smoke) |
| E2 | scaffoldEmiratesId: custom `id` threads through html + js; invalid id rejected by schema | `test/edge-cases.test.mjs` |
| E3 | scaffoldEmiratesId: maskedDisplay:false omits the display block; html always carries the mandatory pattern + dir="ltr" input | `test/edge-cases.test.mjs` |
| E4 | scaffoldUaePass: all 15 variant×appearance combos emit aegov-btn markup that validates clean, wording matches the variant | `test/edge-cases.test.mjs` |
| E5 | scaffoldUaePass: ar-only output is RTL (`dir="rtl"`), en-only carries no Arabic; staging vs production endpoints on the right hosts | smoke (both/en+prod) + `test/` (ar-only) |
| E6 | scaffoldUaePass: invalid enum values rejected by schema | `test/edge-cases.test.mjs` |

## F. validate_snippet — soundness (new; includes F1–F4 regressions)

| # | Criterion | Covered by |
|---|-----------|-----------|
| F1 | EID field identified only via `<label for>` (generic name/id) → error; search box labelled "…Emirates ID…" NOT flagged | `test/` (regression: ADVERSARIAL F1) |
| F2 | Unmasked full-format EID in page content → **error** (valid:false); masked X-form stays valid | `test/` (regression: ADVERSARIAL F2) |
| F3 | Correct pattern in single quotes accepted; wrong pattern value → error | `test/` (regression: ADVERSARIAL F3) |
| F4 | Unquoted `class=aegov-fake` still flagged | `test/` (regression: ADVERSARIAL F4) |
| F5 | Empty html rejected by schema; whitespace-only / plain-text / no-class input returns a well-formed valid result (no crash) | `test/edge-cases.test.mjs` |
| F6 | Non-DLS framework classes (e.g. Bootstrap) → info "unverified", not error (docs tier is best-effort by design) | `test/edge-cases.test.mjs` |
| F7 | Unknown aegov-* class → error with did-you-mean suggestion | smoke (flag) + `test/` (suggestion) |
| F8 | img-without-alt → error; button-without-type → warning; Arabic-without-rtl → warning, with dir="rtl" → silent | `test/edge-cases.test.mjs` |
| F9 | Large input (~200 KB) answers without crash | `test/edge-cases.test.mjs` |

## G. Packaging & publish-readiness (new)

| # | Criterion | Covered by |
|---|-----------|-----------|
| G1 | `npm pack` ships only dist/ + catalog/ (+ package.json, README, LICENSE); no src/scripts/evals/inventory leak; dist/index.js + both catalog JSONs present | `test/packaging.test.mjs` |
| G2 | Installed from the packed tarball into a clean directory, the bin starts and serves ping + a catalogue tool over stdio (catalog path resolution works outside the repo) | `test/packaging.test.mjs` |
| G3 | package.json: MIT, engines node>=18, exact-pinned dependencies (no ^/~), bin present | `test/packaging.test.mjs` |
| G4 | LICENSE file ships in the tarball | `test/packaging.test.mjs` run (verified present, 1.1 kB) |
| G5 | README with the required "Community project. Not affiliated with or endorsed by TDRA." disclaimer | **step 7 — knowingly not written yet** |

## Findings from the first full run (2026-07-10 — all suites green, 58/58 + evals 10/10 + smoke 19/19 + validate OK)

- **T1 (minor, UX of error message):** `validate_snippet`'s did-you-mean suggestion is substring-based
  against package class names, so `aegov-btnn` suggests `aegov-btn` but the docs-name-derived typo
  `aegov-button` gets an error with **no** suggestion ("button" is not a substring of "btn" and vice
  versa). The error itself still fires — compliance is unaffected. Possible later improvement: also
  match against docs names when computing suggestions.
- **T3 (non-negotiable NOT tool-enforced): DMY dates.** CLAUDE.md lists DMY dates as a
  non-negotiable, but no tool checks date formats — an MDY date (`12/31/2026`) passes
  `validate_snippet` silently. Asserted as a KNOWN LIMIT test so a future check must flip it
  deliberately. Decision needed (same class as F2 was): either add a heuristic date check to
  `validate_snippet`, or state that DMY is prompt-guidance only.
- **T4 (non-negotiable partially enforced): tokens over arbitrary values.** `validate_snippet`
  verifies class identity but never inspects `style="..."` — hard-coded hex colours and px values
  pass silently. Enforced today only indirectly (evals + the tools' guidance text). KNOWN LIMIT
  test added; candidate for a Stage-2-auditor-grade check.
- **T5 (design limit, documented): structure is not validated.** Correct aegov-* classes in wrong
  nesting (e.g. `aegov-check-item` outside a form control) validate clean — the tool checks class
  identity (package tier, certain) and heuristics, not DOM structure. Per-screen structure is
  covered by the eval specs' requiredPatterns instead. KNOWN LIMIT test added.
- **T2 (consistency, negligible impact):** the F4 unquoted-`class` regex fix was applied to
  `src/tools/validateSnippet.ts` and `scripts/run-evals.mjs`, but `src/tools/shared.ts`
  (`driftClassesIn`, used for driftWarning annotations on served docs examples) still matches only
  quoted attributes. Docs examples are always quoted, so no observed impact — flagged for
  consistency the next time shared.ts is touched.

## Known out-of-scope for this suite

- Native-speaker Arabic review (human, pending with Alam — flagged in outputs by design).
- Docs drift over time (`needs-revalidation` is by definition a runtime trust flag, not testable now).
- React package (`@aegov/design-system-react`) — out of v1 scope.
